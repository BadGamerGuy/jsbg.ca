<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tap to Pay | JSBG</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            text-align: center;
            padding: 2rem;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.6;
            color: #333;
        }
        .success { 
            color: green;
            font-weight: bold;
            margin: 1rem 0;
        }
        .button {
            background-color: #0366d6;
            color: white;
            padding: 1rem 2rem;
            font-size: 1.2rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            margin: 1rem 0;
            display: inline-block;
            text-decoration: none;
        }
        .button:hover {
            background-color: #0356b6;
        }
        .info-box {
            margin-top: 2rem;
            padding: 1.5rem;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
            text-align: left;
        }
    </style>
</head>
<body>

    <h1>✅ Payment Prepared</h1>
    <p>Your recipient's email address is now copied to your clipboard.</p>
    
    <div id="confirmationMessage" class="success" style="display: none;">
        ✔️ Copied: <strong>shaun@jsbg.ca</strong>
    </div>

    <p>Simply open your banking app to complete the transfer. The amount field will be ready for you to enter.</p>

    <a class="button" href="#" id="appButton">Open Banking App</a>

    <div class="info-box">
        <h3>Next Steps:</h3>
        <ol>
            <li>Tap <strong>"Open Banking App"</strong> above.</li>
            <li>Choose to <strong>"Send Money"</strong> or <strong>"Send an e-Transfer"</strong>.</li>
            <li>When asked for the recipient's email, just <strong>paste</strong>—it's already on your clipboard!</li>
            <li>Enter the amount and send.</li>
        </ol>
        <p><em>Auto-deposit is enabled for <strong>shaun@jsbg.ca</strong>. No security question needed.</em></p>
    </div>

    <script>
        // The bulletproof part: Copy the email to the clipboard automatically.
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(function() {
                console.log('Copying to clipboard was successful!');
                // Show a success message to the user
                const confirmationEl = document.getElementById('confirmationMessage');
                confirmationEl.style.display = 'block';
            }).catch(function(err) {
                console.error('Could not copy text: ', err);
                // If automatic copy fails, do nothing. The user can still manually copy from the page.
            });
        }

        // 1. Define the email address
        const recipientEmail = 'shaun@jsbg.ca';

        // 2. Execute the copy command as soon as the page loads
        window.onload = function() {
            copyToClipboard(recipientEmail);
        };

        // 3. Make the "Open Banking App" button try to deep link, but fail gracefully.
        const appButton = document.getElementById('appButton');
        appButton.addEventListener('click', function(event) {
            event.preventDefault(); // Prevent the link from being a simple "#"
            // Try to open the app. If it fails, the user just remains on the page. No harm done.
            window.location.href = "interac-e-transfer://send?email=" + recipientEmail;
        });
    </script>

</body>
</html>
